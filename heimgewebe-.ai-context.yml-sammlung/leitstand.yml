ai_context_version: 1.0

project:
  name: leitstand
  summary: UI/Dashboard für das Heimgewebe.
  role: observability_control_room
  primary_language: typescript
  visibility: internal

dependencies:
  internal:
    - name: chronik
      relationship: uses
      interface:
        - event_query
        - timeline_view
    - name: semantah
      relationship: uses
      interface:
        - graph_query
        - insight_stream
    - name: hauski
      relationship: uses
      interface:
        - trace_view
        - action_log
  external:
    - name: React
    - name: Vite

architecture:
  entrypoints:
    - apps/web/src/main.tsx
  key_paths:
    - path: src/components/panels
      purpose: UI panels for event views
    - path: src/services/api.ts
      purpose: API clients for backend services

conventions:
  branching: "main, feature/*"
  commit_prefix: "leitstand"
  ci_platform: github_actions

documentation:
  adr: docs/adr/

ai_guidance:
  do:
    - use well-defined data contracts for API communication
    - build reusable UI components
  dont:
    - embed business logic directly in UI components
