---
name: wgx-smoke

"on":
  workflow_call:
  workflow_dispatch:
  push:
    paths:
      - ".wgx/**"
      - "docs/wgx-konzept.md"
      - ".github/workflows/wgx-smoke.yml"

permissions:
  contents: read

jobs:
  smoke:
    # Compatibility filename only: verification policy is owned by Metarepo.
    uses: heimgewebe/metarepo/.github/workflows/reusable-repo-verify.yml@9d985c3a426d387173d8753b09dd5175a242a7d5
    with:
      mode: smoke
    permissions:
      contents: read
