name: CI
on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  smoke:
    uses: ./.github/workflows/wgx-reusable.yml
    with:
      task: smoke

  guard:
    uses: ./.github/workflows/wgx-reusable.yml
    with:
      task: guard
