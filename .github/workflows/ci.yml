---
name: CI

permissions:
  contents: read

on:
  push:
    branches: [main]
  pull_request:

jobs:
  smoke:
    uses: ./.github/workflows/wgx-smoke.yml

  guard:
    uses: ./.github/workflows/wgx-guard.yml
