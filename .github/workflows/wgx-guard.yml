name: wgx-guard

on:
  workflow_call:

permissions:
  contents: read

jobs:
  guard:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Make wgx executable
        run: chmod +x wgx/wgx

      - name: Run guard task
        run: ./wgx/wgx guard
