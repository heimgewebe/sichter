#!/usr/bin/env bash
set -euo pipefail
PROC="$HOME/sichter/review/pr-processed.json"
cp -f "$PROC" "$PROC.bak.$(date +%Y%m%d-%H%M%S)" 2>/dev/null || true
echo '{}' > "$PROC"
echo "✅ Cache geleert: $PROC"
